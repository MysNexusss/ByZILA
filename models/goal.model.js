/**
 * goal.model.js
 * ============================================================================
 * Definição da forma dos dados de uma Meta financeira (futura tabela
 * "goals" no Supabase). Apenas documentação de tipo.
 *
 * @typedef {Object} Goal
 * @property {string} id
 * @property {string} user_id
 * @property {string} title
 * @property {number} target_amount
 * @property {number} current_amount
 * @property {string} deadline        - formato ISO 8601
 * @property {string} created_at      - formato ISO 8601
 *
 * Status: 🚧 Não implementado — fase atual: App Shell (arquitetura).
 * ============================================================================
 */
