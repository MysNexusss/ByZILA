/**
 * card.js
 * ============================================================================
 * Estrutura preparada para renderização dinâmica de cards.
 *
 * Responsabilidades futuras:
 *  - Gerar markup de card a partir de dados (ex.: renderCard(data)),
 *    reutilizando as classes definidas em css/components.css.
 *
 * Status: 🚧 Não implementado — fase atual: App Shell (arquitetura).
 * ============================================================================
 */
