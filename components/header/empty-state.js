/**
 * empty-state.js
 * ============================================================================
 * Estrutura preparada para estados vazios (listas sem dados).
 *
 * Responsabilidades futuras:
 *  - renderEmptyState(container, { titulo, descricao, acao }) — usado
 *    quando transactions.js, goals.js ou debts.js não retornarem itens.
 *
 * Status: 🚧 Não implementado — fase atual: App Shell (arquitetura).
 * ============================================================================
 */
